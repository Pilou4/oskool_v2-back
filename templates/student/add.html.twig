{% extends 'base.html.twig' %}

{% block body %}
<h2 class="mb-5">Ajouter une école</h2>

{{ form_start(studentForm) }} 

    <div class="row">
        <div class="col-sm-6">
            {{ form_row(studentForm.firstname) }}
            {{ form_row(studentForm.lastname) }}
            {{ form_row(studentForm.birthday) }}
            {{ form_row(studentForm.age) }}
            {{ form_row(studentForm.level) }}
            {{ form_row(studentForm.hobbies) }}
            {{ form_row(studentForm.health) }}
            {{ form_row(studentForm.image_right) }}
            <input type="submit" value="Ajouter" class="btn btn-primary">
        </div>
    </div>
    

    
{{ form_end(studentForm) }}

{% endblock %}